## Documentation

### Extracting the keypoints was simple, just needed to copy paste the previous activity.
### Open CV still couldn't install properly so in order to properly debug the other code, I had to remove the OpenCV installation code unlike my code in EXER2.
### Since OpenCV couldn't be installed, SURF was not able to run without errors.

## I noticed from my two matchings is, Brute-Force matched the subject while FLANN matched the background.
